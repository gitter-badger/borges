import React from 'react';

const Footer = React.createClass({
  render() {

    return (
      <footer className="borges-footer">
        footer
      </footer>
    );
  }
});

export default Footer;
